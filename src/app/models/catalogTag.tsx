export interface CatalogTag {
    id_categoria: number
    nombre: string
    icono: string
    estado: number
    idioma: {
        id: number
        nombre: string
        desripcion: string
    }
}
